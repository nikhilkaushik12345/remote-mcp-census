# Remote MCP Server Census

**12,385 verified** · **8,321 OAuth** · **5,063 OAuth root domains** · **4,525 DCR**

Live protocol verification only (MCP `initialize` + RFC 9728/8414).

## Files
- `remote_mcp_servers.csv` / `.json` — full verified set
- `remote_mcp_oauth.csv` — OAuth-confirmed hosts
- `remote_mcp_oauth_root_domains.csv` — one host per company root
- `index.html` · `evidence/` · `scripts/`

Latest: Tranco 100k–350k apex spray + PRM well-known spray + alt-prefix DoH.
+1,530 hosts (+1,158 OAuth). Quality finds include Bloomberg, Quizlet, Capacities, Expedia MCP Gateway, Luzmo, OneSignal, Outline, Dovetail, Patchstack, Teslemetry, Token Terminal, and more.
