# Remote MCP Server Census

**8,907 verified** · **6,068 OAuth** · **4,144 DCR** · **root domains in `remote_mcp_oauth_root_domains.csv`**

Live MCP `initialize` + RFC 9728/8414 only. No directories.

## Latest: Method Z
PRM (`/.well-known/oauth-protected-resource`) + apex `/mcp` spray on brand/api/www hosts
(non-`mcp.` subdomain shape). +41 new hosts (nvidia, tableau, retool, tray, mux, socket, missive, heap, productboard, airbnb dev, …).

## Files
| File | What |
|---|---|
| `remote_mcp_servers.csv` | All verified |
| `remote_mcp_oauth.csv` | OAuth confirmed |
| `remote_mcp_oauth_root_domains.csv` | Unique company domains (OAuth) |
| `index.html` | Report |
| `evidence/` | Raw probes |
| `scripts/` | Pipeline |
